{
  "name": "blog-vue-press",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docs:dev": "npm run entry && vuepress dev docs",
    "docs:build": "vuepress build docs",
    "entry": "node ./prepare.js",
    "ci": "npm install && npm run entry && npm run docs:build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vuepress": "^2.0.0-beta.27",
    "@vuepress/shared-utils": "^1.8.2",
    "@vue/component-compiler-utils": "^3.3.0",
    "@vuepress/plugin-search": "^1.8.2"
  }
}
